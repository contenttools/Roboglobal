<%= form_for @attachment, :html => { :multipart => true }, authenticity_token: true do |form| %>
  <% if @attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% @attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.file_field :image, required: true %>
  <%= form.submit submit_text(form.object, "Image"), :class => 'btn btn-success' %>
<% end %>
